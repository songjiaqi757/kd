diff --git a/run.py b/run.py
index 4984aa4..f829a8b 100644
--- a/run.py
+++ b/run.py
@@ -100,6 +100,8 @@ def DLF_run(
         args['cur_seed'] = i + 1
         result = _run(args, num_workers, is_tune)
         model_results.append(result)
+    if args.get('return_sample_results', False):
+        return model_results
     if args.is_training:
         criterions = list(model_results[0].keys())
         # save result to csv
@@ -120,6 +122,7 @@ def DLF_run(
         df.loc[len(df)] = res    
         df.to_csv(csv_file, index=None)
         logger.info(f"Results saved to {csv_file}.")
+    return model_results
 
 
 def _run(args, num_workers=4, is_tune=False, from_sena=False): 
@@ -189,13 +192,19 @@ def _run(args, num_workers=4, is_tune=False, from_sena=False):
         input('[Press Any Key to start another run]')
     #train
     else:
-        epoch_results = trainer.do_train(model, dataloader, return_epoch_results=from_sena)
-        model[0].load_state_dict(torch.load('./pt/DLF'+str(args.dataset_name)+'.pth'))
-
-        results = trainer.do_test(model[0], dataloader['test'], mode="TEST")
+        if args.get('train_model', True):
+            epoch_results = trainer.do_train(model, dataloader, return_epoch_results=from_sena)
+        checkpoint_path = Path(args.get('checkpoint_path', args.model_save_path))
+        model[0].load_state_dict(torch.load(checkpoint_path))
+        evaluation_split = 'test' if args.get('evaluate_test', False) else 'valid'
+        results = trainer.do_test(
+            model[0], dataloader[evaluation_split], mode=evaluation_split.upper(),
+            return_sample_results=args.get('return_sample_results', False),
+        )
+        results['DeployedParameters'] = sum(parameter.numel() for parameter in model[0].parameters())
 
         del model
         torch.cuda.empty_cache()
         gc.collect()
         time.sleep(1)
-    return results
\ No newline at end of file
+    return results
diff --git a/trains/singleTask/DLF.py b/trains/singleTask/DLF.py
index 41e2f06..4db296e 100644
--- a/trains/singleTask/DLF.py
+++ b/trains/singleTask/DLF.py
@@ -162,25 +162,19 @@ class DLF():
             )
             # validation
             val_results = self.do_test(model[0], dataloader['valid'], mode="VAL")
-            test_results = self.do_test(model[0], dataloader['test'], mode="TEST")
             cur_valid = val_results[self.args.KeyEval]
             scheduler.step(val_results['Loss'])
-            # save each epoch model
-            torch.save(model[0].state_dict(), './pt/' + str(self.args.dataset_name) + '_' + str(epochs) + '.pth')
             # save best model
             isBetter = cur_valid <= (best_valid - 1e-6) if min_or_max == 'min' else cur_valid >= (best_valid + 1e-6)
             if isBetter:
                 best_valid, best_epoch = cur_valid, epochs
                 # save model
-                model_save_path = './pt/DLF' + str(self.args.dataset_name)+'.pth'
-                torch.save(model[0].state_dict(), model_save_path)
+                torch.save(model[0].state_dict(), self.args.model_save_path)
 
             if return_epoch_results:
                 train_results["Loss"] = train_loss
                 epoch_results['train'].append(train_results)
                 epoch_results['valid'].append(val_results)
-                test_results = self.do_test(model, dataloader['test'], mode="TEST")
-                epoch_results['test'].append(test_results)
             # early stop
             if epochs - best_epoch >= self.args.early_stop:
                 return epoch_results if return_epoch_results else None
@@ -230,4 +224,4 @@ class DLF():
             eval_results['Features'] = features
             eval_results['Labels'] = all_labels
 
-        return eval_results
\ No newline at end of file
+        return eval_results
